# 마켓 캘린더 CHECK — 설치 및 운영 설명서 (9호 배포 도구)

한국·미국 시장의 투자 일정을 한 화면에 모아 **사전 준비 항목까지 체크**하는 영업지원 사이트입니다.
자사주 CHECK · 내부자 매매 CHECK · 지수 정기변경 CHECK 와 동일한 방식(GitHub Pages + Actions)으로 운영합니다.

- 저장소명(권장): `Market-Calendar-Korea`
- 공개 주소(예정): `https://bluelagoon1222.github.io/Market-Calendar-Korea/`

---

## 1. 파일 구성

```
index.html                     화면 전체 (단일 파일, 외부 라이브러리 없음)
data/fixed.js / fixed.json     고정 일정 (휴장·통화정책·만기·리밸런싱·지표·세무)
data/earnings.js / .json       실적발표 일정 (수집 결과)
data/corp.js / .json           국내 기업행위 일정 (수집 결과)
data/status.js / .json         수집 실행 로그 (건수·오류)
scripts/build_fixed.py         고정 일정 생성 (네트워크 불필요)
scripts/collect.py             실적·기업행위 수집 (네트워크 + DART 인증키)
.github/workflows/update.yml   평일 07:40 / 17:50 KST 자동 실행
```

## 2. 업로드 절차

1. GitHub 에서 새 저장소 `Market-Calendar-Korea` 생성 (Public).
2. `index.html`, `data` 폴더, `scripts` 폴더를 웹 화면에서 드래그 업로드.
3. **`.github/workflows/update.yml` 은 드래그 업로드로 빠집니다**(점으로 시작하는 폴더).
   저장소 상단 `Add file → Create new file` 을 눌러 파일명 칸에
   `.github/workflows/update.yml` 을 직접 입력하고 내용을 붙여넣습니다.
4. `Settings → Pages → Source: Deploy from a branch / main / (root)` 저장.
5. `Settings → Secrets and variables → Actions → New repository secret`
   - Name: `DART_API_KEY`
   - Secret: 자사주 CHECK 에서 쓰는 것과 같은 인증키
6. `Actions → update-calendar → Run workflow` 로 1회 수동 실행.

인증키가 없어도 화면은 정상 동작합니다(고정 일정 + 미국 실적만 표시).

## 3. 화면 사용법

| 기능 | 설명 |
|---|---|
| 월간 | 달력 격자. 휴장일은 셀에 `한국 휴장`/`미국 휴장` 배지로 표시 |
| 일정 리스트 | 향후 60일을 날짜별로, D-Day 와 함께 나열 |
| 실적 캘린더 | 주(週) 단위로 묶은 실적발표 표. 장전/장마감 후, 컨센서스 EPS 표시 |
| 한국 / 미국 | 시장 필터. 글로벌 이벤트(FOMC 외 중앙은행 등)는 항상 표시 |
| 카테고리 칩 | 실적발표·지수·통화정책·경제지표·휴장·기업행위·세무·이벤트 on/off |
| 핵심만 | 중요도 ★(3단계 중 최상) 일정만 남김 — 아침 회의용 |
| 확정만 | 규칙·과거 패턴으로 추정한 '잠정' 일정을 숨김 — 고객 안내 직전에 사용 |
| 일정 클릭 | 상세 + **사전 준비 체크리스트**(체크 상태는 브라우저에 저장) + 원문 링크 |
| .ics 내보내기 | 필터된 향후 120일 일정을 아웃룩·구글 캘린더로 반입 |
| 안내용 문구 복사 | 일정·요점·체크포인트를 텍스트로 복사 (카톡·메일 붙여넣기용) |

## 4. 데이터 출처와 한계 (고객 안내 전 반드시 확인)

| 항목 | 출처 | 확정/잠정 |
|---|---|---|
| 한국·미국 휴장일, 조기폐장, 서머타임 | KRX / NYSE 공표 | 2026년 확정, 2027년 잠정 |
| FOMC·의사록·블랙아웃 | Federal Reserve | 회의일 확정, 의사록은 규칙(+21일) |
| 한국은행 금통위 | 한국은행 2026년 일정 | 확정 (의사록은 관행) |
| 선물·옵션 만기 | KRX(둘째 목), 미국(셋째 금) | 규칙 기반 확정 |
| S&P·나스닥100·MSCI·FTSE 리밸런싱 | 각 지수사 규칙 | 적용일은 규칙, 편입·편출 종목은 발표 후 확인 |
| KRX 정기변경 | KRX 규정(만기 다음 거래일) | 적용일 확정, 종목은 11월 발표 후 |
| 미국 실적발표일·장전/장후 | Nasdaq Earnings Calendar, Yahoo | 확정(회사 공지 기준), 미확정은 '잠정' |
| 국내 실적발표일 | DART 결산실적공시예고 → 확정 / 전년 동기 공시일 → 추정 | 혼재 (화면에 구분 표시) |
| 국내 증자·CB·BW·자사주 일정 | DART 주요사항보고서 | 확정 (공시 원문 링크 제공) |
| 미국 CPI·PCE·ISM | 규칙 기반 추정 | 잠정 — BLS 공표 일정으로 대체 예정 |

**국내 실적발표의 장전/장후 구분은 미국 대비 정확도가 낮습니다.** 국내는 발표 시각을 사전에
공표하는 제도가 없어, 예고 공시 본문에 '장 종료 후' 등이 명시된 경우에만 확정 표시됩니다.
그 외에는 `미정` 으로 표시하며, 국내 잠정실적은 통상 장중~장 마감 후에 공시됩니다.

## 5. 확장 시 손댈 곳

- 고정 일정 추가·수정 → `scripts/build_fixed.py` 의 해당 리스트(휴장일·세무·이벤트)만 수정
- 수집 유니버스 변경 → `collect.py` 의 `KRX_IDX`(지수), `us_universe()`(S&P500·나스닥100)
- 카테고리 추가 → `index.html` 의 `CAT` 객체 + `I18N` 의 `cat` 라벨
